name: Echo on Push

on:
  push:
    branches:
      - main  # トリガーするブランチを指定

jobs:
  echo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo a message
        run: echo "Hello, this is a test message!"
